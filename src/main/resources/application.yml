spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:mariadb://localhost:3306/wasser
    username: wasser
    password: wasser
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  liquibase:
    enabled: false
server:
  port: 8081
